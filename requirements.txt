# ============================================================
# requirements.txt - AI_translation Project
# ============================================================
# جميع التبعيات المطلوبة للمشروع
# يعمل على Python 3.12 و Google Colab مع GPU T4
# ============================================================

# ------------------------------------------------------------
# إطار عمل تيليجرام
# ------------------------------------------------------------
pyrogram==2.0.106
tgcrypto==1.2.5

# ------------------------------------------------------------
# async و event loop
# ------------------------------------------------------------
nest_asyncio==1.6.0

# ------------------------------------------------------------
# أدوات الفيديو والصوت
# ------------------------------------------------------------
yt-dlp>=2024.8.6
ffmpeg-python==0.2.0

# ------------------------------------------------------------
# Whisper (OpenAI) للتفريغ الصوتي
# ------------------------------------------------------------
openai-whisper==20231117

# ------------------------------------------------------------
# HuggingFace Transformers و PyTorch لـ Qwen2.5-3B-Instruct
# ------------------------------------------------------------
transformers>=4.44.0
torch>=2.0.0
torchvision>=0.15.0
accelerate>=0.30.0
sentencepiece>=0.2.0
protobuf>=5.0.0
safetensors>=0.4.0
tokenizers>=0.19.0
huggingface_hub>=0.24.0

# ------------------------------------------------------------
# معالجة النصوص والترجمة
# ------------------------------------------------------------
regex>=2024.7.24

# ------------------------------------------------------------
# أدوات مساعدة
# ------------------------------------------------------------
aiofiles>=24.1.0
python-dotenv==1.0.1

# ------------------------------------------------------------
# واجهة الويب (لا تستخدم في البوت، لكن مفيدة للمراقبة في Colab)
# ------------------------------------------------------------
# gradio>=4.0.0

# ------------------------------------------------------------
# ملاحظات التثبيت:
# ------------------------------------------------------------
# 1. لتثبيت FFmpeg على Ubuntu/Colab:
#    sudo apt-get update && sudo apt-get install -y ffmpeg
#
# 2. لتثبيت PyTorch مع دعم CUDA (لـ Colab GPU):
#    pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#
# 3. لإصلاح مشاكل asyncio في Colab:
#    pip install nest_asyncio
#
# 4. لتفادي أخطاء protobuf مع Qwen:
#    pip install --upgrade "protobuf>=5.0.0"
# ============================================================
